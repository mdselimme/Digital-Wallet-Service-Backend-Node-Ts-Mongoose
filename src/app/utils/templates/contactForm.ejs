<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Contact Form Submission</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gray-100 py-10">
    <div
      class="max-w-2xl mx-auto bg-white shadow-lg rounded-2xl overflow-hidden"
    >
      <!-- Header -->
      <div class="bg-gradient-to-r from-indigo-600 to-purple-600 px-6 py-4">
        <h4 class="text-lg font-bold text-white">✉ New Email From DigiPay</h4>
      </div>

      <!-- Body -->
      <div class="p-6 space-y-4">
        <p class="text-gray-700 text-base">
          You&apos;ve received a new message from your website contact form.
        </p>

        <div class="grid grid-cols-1 gap-4">
          <!-- Name -->
          <div class="border rounded-lg p-4 bg-gray-50">
            <p class="text-sm text-gray-500 font-semibold">
              Name:
              <span class="text-gray-800 text-lg font-medium"><%= name %></span>
            </p>
          </div>

          <!-- Email -->
          <div class="border rounded-lg p-4 bg-gray-50">
            <p class="text-sm text-gray-500 font-semibold">
              Email:
              <span class="text-gray-800 text-lg font-medium"
                ><%= email %></span
              >
            </p>
          </div>

          <!-- Subject -->
          <div class="border rounded-lg p-4 bg-gray-50">
            <p class="text-sm text-gray-500 font-semibold">
              Subject:
              <span class="text-gray-800 text-lg font-medium"
                ><%= subject %></span
              >
            </p>
          </div>

          <!-- Message -->
          <div class="border rounded-lg p-4 bg-gray-50">
            <p class="text-sm text-gray-500 font-semibold">
              Message:
              <span class="text-gray-800 text-lg font-medium"
                ><%= message %></span
              >
            </p>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
